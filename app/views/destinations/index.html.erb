<div class="min-h-screen bg-gray-100 flex flex-col items-center px-4 pt-10">
  <div class="bg-white w-full rounded-3xl shadow-lg overflow-hidden">
    <div class="px-6 py-4 border-b border-gray-200 flex justify-between items-center">
      <h2 class="text-2xl font-semibold text-gray-800">Destinos</h2>
     <%= link_to "Nuevo destino", new_destination_path, data: { turbo_frame: "modal" },
          class: "bg-gray-800 text-white px-4 py-2 rounded-lg hover:bg-gray-700 transition font-medium" %>

    </div>

    <%= turbo_frame_tag "destinations" do %>
      <%= render "table", destinations: @destinations %>
    <% end %>
  </div>
</div>

<%= turbo_frame_tag "modal" %>